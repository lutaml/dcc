# Certs

The certificates in this folder are for testing purposes only. They are not valid and should not be used in production.

The certificates are self-signed and have been generated using the following command:

```bash
./issue_certs.sh
```
