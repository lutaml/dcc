# frozen_string_literal: true

module Dcc
  module Builder
    # Placeholder module — the DSL is implemented directly on `Session`.
    # Kept for future expansion (e.g. shared mixins for V2 vs V3 DSL).
    module Dsl
    end
  end
end