# frozen_string_literal: true

# `Dcc::Si::Base` hosts shared XML mapping modules for D-SI elements.
# One file per D-SI complexType, following the same `self.included(klass)`
# pattern as `Dcc::Base`.
module Dcc
  module Si
    module Base
      # autoloads added per-type in subsequent phases
    end
  end
end
